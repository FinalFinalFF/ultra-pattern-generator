{
  "name": "grid-pattern-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run copy-ffmpeg && vite",
    "build": "npm run copy-ffmpeg && tsc && vite build",
    "preview": "vite preview",
    "copy-ffmpeg": "node scripts/copy-ffmpeg-core.mjs"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.2.1"
  }
}
